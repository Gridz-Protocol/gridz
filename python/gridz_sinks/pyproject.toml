[project]
name = "gridz-sinks"
version = "0.1.0"
description = "Gridz sink adapters (Python)."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = ["gridz"]

[project.optional-dependencies]
dev = ["pytest>=8.3.4", "pytest-cov>=6.0.0"]

[tool.uv.sources]
gridz = { path = "../gridz", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gridz_sinks"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=gridz_sinks --cov-report=term-missing --cov-fail-under=90"
