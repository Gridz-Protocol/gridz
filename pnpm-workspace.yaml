packages:
  - "packages/*"
  # Only workspace-buildable examples here. next-app and scaffold-agent-grid are
  # standalone reference apps (their own installs), excluded to keep CI light.
  - "examples/minimal-cli"
  - "examples/oneclaw-quickstart"
